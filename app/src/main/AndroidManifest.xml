<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ph.edu.dlsu.mobdeve.s18.flores.miguel.mtgmimic">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MTGMimic">
        <activity android:name=".DeckStatActivity"></activity>
        <activity android:name=".DeckDetailsSocialActivity" />
        <activity android:name=".CardStatsActivity" />
        <activity android:name=".CardDetailsActivity" />
        <activity android:name=".DeckSocialActivity" />
        <activity android:name=".UserDecksActivity" />
        <activity android:name=".UserInventoryActivity" />
        <activity android:name=".MasterCardlistActivity" />
        <activity android:name=".LoginPageActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>